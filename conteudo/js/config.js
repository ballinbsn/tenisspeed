/**
 * Configuração central do advertorial.
 * Troque PRODUCT_URL quando a landing page final estiver pronta —
 * todos os CTAs da página usam esta mesma constante.
 */
window.SITE_CONFIG = {
  PRODUCT_URL: "https://tenispeed.shop/",
};
